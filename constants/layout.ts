import { spacing } from '@/theme/spacing';

/** Extra space below the system status bar / notch (in addition to safe area inset). */
export const SCREEN_EXTRA_TOP_PADDING = spacing[3];

/** Default horizontal screen gutter. */
export const SCREEN_HORIZONTAL_PADDING = spacing[6];
